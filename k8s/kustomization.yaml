apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: peadminns
resources:
  - 00-namespace.yaml
  - 01-configmap.yaml
  - 03-backend.yaml
  - 04-frontend.yaml
  - 05-ingress.yaml
# 02-secret.example.yaml is intentionally excluded — create the real
# secret with `kubectl create secret` (see README.md), don't apply the
# template file.
